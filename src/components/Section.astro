---
const { title, class: className } = Astro.props
---

<section class:list={["max-w-3xl mt-0 mx-auto mb-12 max-[700px]:mb-9 w-full",className]}>
  {title && <h2 class="mb-2 font-bold text-2xl">{title}</h2>}
  <slot />
</section>